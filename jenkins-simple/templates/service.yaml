apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-simple.fullname" . }}
  labels:
    {{- include "jenkins-simple.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-simple.selectorLabels" . | nindent 4 }}
