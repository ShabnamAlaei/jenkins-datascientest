apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jenkins-simple.fullname" . }}-test-connection"
  labels:
    {{- include "jenkins-simple.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jenkins-simple.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
